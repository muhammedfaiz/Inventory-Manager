{
    "compilerOptions": {
      "baseUrl": ".",  // Base directory (root of the project)
      "paths": {
        "@/*": ["./src/*"]  // Alias @ to the src folder
      }
    },
    "include": ["src/**/*"]
  }
  